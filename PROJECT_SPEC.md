# SAGE-Price 설계 메모

README에는 큰 흐름만 적고, 여기에는 코드에서 실제로 사용한 데이터와 계산 방식을 남겼습니다. 기본 예측 대상은 종목의 252거래일 뒤 주가입니다.

## 입력 데이터

종목 가격은 Yahoo Finance에서 내려받거나 날짜와 가격이 들어 있는 CSV로 입력합니다. 인터넷 없이 구조만 확인할 때는 합성 데이터를 쓸 수 있습니다. 국내 종목의 시장지표는 `^KS11`, 그 외에는 `^GSPC`를 기본값으로 사용합니다.

규모·가치 요인은 정식 Fama-French 요인 자료가 아닙니다. 코드에서는 `IWM`과 `SPY`의 로그수익률 차이를 규모 요인, `IVE`와 `IVW`의 로그수익률 차이를 가치 요인의 대리지표로 둡니다. 거시 요인은 미국 10년물 금리 지표 `^TNX`의 변화와 `KRW=X`의 환율 수익률을 사용합니다. 자료를 가져오지 못하면 해당 요인을 0으로 처리하거나 기본 가정값으로 계산합니다.

## 다섯 모형

- **CAPM:** 종목과 시장의 과거 수익률로 베타를 추정하고 시장 초과수익률을 반영합니다.
- **Fama-French 방식 3요인:** 시장 수익률에 위의 규모·가치 대리지표를 더해 회귀합니다. 정식 한국 SMB·HML을 사용한 결과는 아닙니다.
- **APT 방식 거시요인:** 시장 수익률, 금리 변화, 환율 수익률에 대한 종목 수익률의 관계를 추정합니다.
- **배당할인모형:** 최근 배당금과 배당 성장률로 가치를 계산합니다. 배당 자료가 없으면 기본 배당수익률을 사용합니다.
- **잔여이익모형:** 장부가치와 자기자본이익률을 이용합니다. 장부가치를 입력하지 않으면 현재가와 기본 P/B 가정으로 대신합니다.

모형을 이렇게 나눈 이유는 시장 위험, 거시 변수, 배당, 회계이익이 서로 다른 정보를 담기 때문입니다. 같은 종목에서 계산값이 어느 정도 달라지는지도 비교할 수 있습니다.

## 과거 오차를 비중으로 바꾸는 방법

기본 설정은 처음 3년의 가격을 확보한 뒤 252거래일 간격으로 기준일을 옮기고, 각 기준일에서 252거래일 뒤의 주가를 추정합니다. 모형별 예측 오차는 아래처럼 기준일 주가로 나눠 비교합니다.

```text
상대오차 = |실제 주가 - 모형의 예상주가| / 기준일 주가
모형 점수 = 1 / (평균 상대오차 + 아주 작은 값)
모형 비중 = 모형 점수 / 다섯 모형 점수의 합
최종 기대주가 = Σ(모형별 예상주가 × 모형 비중)
```

하단·상단 가격에는 모형별 구간의 평균을 쓰지 않았습니다. 과거 결합 예측의 `|실제 주가 - 결합 예상주가| / |결합 예상주가|`에서 80분위수를 구해 최종 기대주가에 더하고 뺐습니다. `0.80`은 이 분위수를 정하는 설정값이지, 검증된 확률적 보장 수준이 아닙니다.

실행 결과는 `outputs/`에 최종 요약, 모형별 예측·비중, 과거 시점별 비교표로 저장됩니다. 구현 위치는 [`models.py`](stock_range_model/models.py), [`backtest.py`](stock_range_model/backtest.py), [`ensemble.py`](stock_range_model/ensemble.py)입니다.

## 다시 검증해야 할 부분

현재 무위험금리는 내려받은 기간의 마지막 `^TNX` 값을 과거 검증 시점에도 공통으로 적용합니다. 과거 기준일 이후의 정보가 섞일 수 있으므로, 기준일별로 그때 확인 가능했던 금리를 사용하도록 고쳐야 합니다.

또한 모형 비중을 전체 과거 결과에서 한 번 정한 다음 같은 기간의 결합 오차를 계산합니다. 이 결합 오차를 외표본 성능으로 볼 수 없습니다. 연 1회 기준일을 잡아 검증 횟수도 많지 않습니다. 이후에는 국내 요인 자료를 구성하고, 기준일마다 비중을 다시 추정하는 검증으로 확장할 생각입니다.
